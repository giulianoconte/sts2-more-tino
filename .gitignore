memory/
agents.md
CLAUDE.md
agents.private.md
MoreTino/libs/

# Drafted commit messages — written into `commit`, `commit1`, `commit2`, …
# at repo root, never committed themselves. Convention shared with sts2-docs.
commit*

# Captured Godot session logs (from redeploy.sh / watch-log.sh) and the
# watcher's PID/output files.
logs/

# Release archives
MoreTino-*.zip

# .NET build outputs
**/bin/
**/obj/

# NuGet
*.nupkg
*.snupkg
**/packages/

# Rider / VS / VS Code
.idea/
.vs/
.vscode/
*.user
*.suo
*.userprefs

# Local overrides
local.props

# Godot
*.import
.godot/
MoreTino/dist/

# Build-copy targets when ModsPath resolves to project-relative.
MoreTino/MoreTino/MoreTino.dll
MoreTino/MoreTino/MoreTino.pck
MoreTino/MoreTino/MoreTino.json
MoreTino/BaseLib/

# OS
.DS_Store
Thumbs.db
